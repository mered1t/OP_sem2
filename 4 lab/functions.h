#pragma once

#include "matrix.h"
#include <string>
#include <time.h>

int chooseM(int);
bool isNumber(const std::string&);
Matrix GenerateMatrix(int, Matrix&);
void Info();
std::string onlyInt(int);

